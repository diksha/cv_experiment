const ACTION_BUTTON_WIDTH_PX = 50;
const ACTION_BUTTON_OFFSET_PX = 10;
const ACTION_BUTTON_MARGIN_PX = ACTION_BUTTON_WIDTH_PX + ACTION_BUTTON_OFFSET_PX;
const ACTION_BUTTON_PADDING_PX = 12;
const ACTION_BUTTON_ICON_WIDTH_PX = ACTION_BUTTON_WIDTH_PX - ACTION_BUTTON_PADDING_PX * 2;

export const ACTION_BUTTON_WIDTH = `${ACTION_BUTTON_WIDTH_PX}px`;
export const ACTION_BUTTON_OFFSET = `${ACTION_BUTTON_OFFSET_PX}px`;
export const ACTION_BUTTON_MARGIN = `-${ACTION_BUTTON_MARGIN_PX}px`;
export const ACTION_BUTTON_PADDING = `${ACTION_BUTTON_PADDING_PX}px`;
export const ACTION_BUTTON_ICON_WIDTH = `${ACTION_BUTTON_ICON_WIDTH_PX}px`;
