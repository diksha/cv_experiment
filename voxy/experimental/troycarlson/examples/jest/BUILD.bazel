load("@aspect_rules_jest//jest:defs.bzl", "jest_test")

jest_test(
    name = "test",
    config = "//:jest_config",
    data = [
        "foo.js",
        "foo.test.js",
    ],
    node_modules = "//:node_modules",
)
