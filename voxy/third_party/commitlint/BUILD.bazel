load("@npm//:commitlint/package_json.bzl", "bin")

bin.commitlint_binary(
    name = "commitlint",
    data = [
        "//:node_modules/@commitlint/config-conventional",
        "//:node_modules/commitlint",
    ],
)
